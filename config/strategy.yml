# config/strategy.yaml
portfolio:
  top_n: 5           # number of tickers to hold
  rebal_freq: ME     # ME = month-end
  cost_bps: 10       # 10 basis points per rebalance trade
  cash_buffer: 0.00  # leave 0% in cash

signals:
  ma:
    fast: 10         # default fast MA
    slow: 50        # default slow MA
    use_crossover: true
